# Example environment file
# To use the worker subdomain directly as the API base, set:
# VITE_API_BASE_URL=https://med-manager-api-proxy.workers.dev

# If omitted, the app will call /api (same-origin) which requires the worker route
# be mapped to your site (recommended for production).